[project]
name = "sam3-vastai-app"
version = "0.1.0"
description = "SAM3 Segmentation API Service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "decord>=0.6.0",
    "einops>=0.8.2",
    "fastapi>=0.128.0",
    "numpy<2",
    "pillow>=12.1.0",
    "psutil>=7.2.2",
    "pycocotools>=2.0.11",
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
    "python-multipart>=0.0.22",
    "sam3[notebook]",
    "torch>=2.10.0",
    "torchvision>=0.25.0",
    "uvicorn>=0.40.0",
]

[tool.uv.sources]
sam3 = { git = "https://github.com/facebookresearch/sam3.git" }

[project.scripts]
serve = "uvicorn main:app --host 0.0.0.0 --port 8000"
